name: Build Workflow

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - uses: actions/checkout@v2
        run: echo "Hello world"

      - uses: actions/checkout@v2
        run: echo "Hello world1"

      - uses: actions/checkout@v2
        run: echo "Hello world1"
